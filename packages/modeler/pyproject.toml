[project]
name = "modeler"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "domain",
    "store",
    "numpy>=1.26.0",
    "scipy>=1.12.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/modeler"]
